This program reads infix arithmetic expresions from a text file, one from each line, then using a queue and stack, prints
the equivalent postfix expressions, asks the user for integer or float values for variables in the expression as keyboard
input, then evaluates the expression and prints the total value.